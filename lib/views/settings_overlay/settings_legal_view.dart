import 'package:flutter/material.dart';

class SettingsLegalView extends StatelessWidget {
  const SettingsLegalView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Legal');
  }
}
