import 'package:flutter/material.dart';

class SettingsHelpView extends StatelessWidget {
  const SettingsHelpView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Help');
  }
}
