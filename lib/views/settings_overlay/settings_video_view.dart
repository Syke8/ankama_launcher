import 'package:flutter/material.dart';

class SettingsVideoView extends StatelessWidget {
  const SettingsVideoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Video');
  }
}
